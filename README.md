Live Link : https://swift-cart-e-commerce-project.vercel.app/

GitHub Repository: https://github.com/Sabbirahmed0007/swiftCart-E-commerce-project




1) What is the difference between null and undefined?
Answer: Null and undefined both are premitive data type. Both don't contains values. Undefined means when the variable is declared but value not assigned. In other words default missing value . And null defines absence of value. the variable is declared but as null.

2) What is the use of the map() function in JavaScript? How is it different from forEach()?
Answer: map() and forEach() both are ES6 method. map() iterate through the array and return a value and it doesn't modify the original array. forEach() also iterate through array but doesn't return anything .forEach() is mostly used for managing side Effects

3) What is the difference between == and ===?
Answer: (==) Double equal checks the vlaue matches or not , It doesn't check the data type. (===) Triple equal mainly checks bothe the value and the data type.

4) What is the significance of async/await in fetching API data?
Answer:  async/await makes actually asynchronous code  like snchronous code. Moreover it improves the readability and very efficient at error handling. It gives cleaner synteax and easier debugging besides.

5) Explain the concept of Scope in JavaScript (Global, Function, Block).
Answer: Scope is a area of a program where a variable , function or an object can be accessed. And Global scope which is declared outside of all function or loops. It is accessible from anywhere of the program. Function scope means a variable is declared inside a funtion and it accessible of within that fucntion . Block scope is mainly declared only using let and const keys . block scope means a variable is declared within the blocks like if/else or loops and only accessible within them











